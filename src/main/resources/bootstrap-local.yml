spring:
  profiles: local
  cloud:
    config:
      server:
        native:
          search-locations: file:///
eureka:
  client:
    register-with-eureka: true
    fetch-registry: false
    service-url:
      defaultZone: http://newkayak.iptime.org:9999/eureka/
  dashboard:
    path: /dashboard
