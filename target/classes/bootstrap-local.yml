spring:
  application:
    name: configuration-server
  profiles:
    active: local
  cloud:
    config:
      server:
        composite:
          - type: native
            search-locations: file:C:\study\config\src\config-template
        bootstrap: true
  config:
    activate:
      on-profile: local
